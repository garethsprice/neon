/**
 * Neon AI - Detect Genre Prompt
 *
 * Template for detecting musical genre from user prompts.
 * Placeholder: {{PROMPT}} - The user's input prompt
 */

export const DETECT_GENRE_PROMPT =
  "Analyze this music prompt and identify the primary genre. Prompt: '{{PROMPT}}'\nReply with ONLY the genre name in lowercase (e.g., techno, house, trance, dnb).";
